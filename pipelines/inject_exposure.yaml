description: Single-frame-level source injection into exposure dataset types.
tasks:
  injectExposure:
    class: lsst.source.injection.inject_exposure.ExposureInjectTask
    config:
      external_psf: true
      external_photo_calib: true
      external_wcs: true
